import { Header } from "../components/header";
import { Layout } from "../components/layout";

const MainScreen = () => {
  return (
    <Layout>
      <Header />
    </Layout>
  );
};

export default MainScreen;
